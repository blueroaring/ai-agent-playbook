# 更新日志

本仓库由维护者的 Agent 定期（默认每 3 天）蒸馏 + 推送。只记"知识层面"的变化，
不记机械同步。格式参考 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)（英文版可永久链接，中文版未做版本化）。

## [0.17.0] - 2026-09-18

同一个 Godot 2D 项目给地图家具批量加"按 E 查看"，一次踩齐三类交互坑。

### 新增

- `lessons/09` 新增第 18 节：**场景交互三连坑**。
  ① **目标自己有碰撞时，交互区必须往外扩** —— 玩家进不去带碰撞的家具/墙体，
  "交互区 = 占地"等于按不动（挂在墙上的窗户/门尤其要往房间内侧延伸）；
  判据是"提示框显示了 [E] 查看 XX 但按了没反应"。
  ② **交互物播完对话不要无脑解锁玩家** —— 一律 `set_control_enabled(true)` 会把
  **剧情锁**一起解开（开场"刚醒只能原地按 E"被一次普通查看静默破坏）。
  正解是在基类做一对 `begin_dialogue_lock()/end_dialogue_lock(was_moving, force_unlock)`，
  先记状态再原样恢复；只有"交互本身推进剧情"才 `force_unlock`。
  ③ **测"够不够得到"要断言"在候选列表里"，不是"就是当前目标"** ——
  物件一多，最近的那个会随位置微调而变；而且引擎常会**保留玩家手动切过的目标**，
  陈旧目标会一直粘着，所以"按 E"之前要先显式把目标切过去。

## [0.16.0] - 2026-09-18

同一个 Godot 2D 项目做"地图上的可拾取道具"，踩到一个**只在第二次进场景才出现**的持久化坑。

### 新增

- `lessons/09` 新增第 17 节：**运行期生成的场景物件，"已消耗"状态必须进存档**。
  可拾取道具由组装脚本按数据 `new()` 出来，销毁它只把"已被拿走"写进了当前场景的内存，
  场景一重建就照数据再生成一个 —— 表现是"捡走的东西离开场景再回来又出现了"，
  反复刷还能叠加成好几个。给出两层解法：
  ① 拿走时把"已拾取"写进存档字典（键由数据显式给出，别用节点名），**生成前先查、
  已拿过就根本不生成**；② 给道具表加 `unique` 标记，入库接口拒收第二件，
  顺带治愈老存档里已经攒下的重复件。
  验证要点：必须挑一条**真实会重进场景**的路径断言（本例 宿舍→校园→宿舍），
  只断言"捡走那一瞬间节点消失了"抓不住它。

## [0.15.0] - 2026-09-18

同一个 Godot 2D 项目继续做"背包系统 + 把地图上的东西改成可拾取物"，
踩到一个**报错点在调用方、根因在引擎机制**的悬空引用坑。

### 新增

- `lessons/09` 新增第 16 节：**"已释放实例"会跨函数边界传播，报错点却在调用方**。
  Godot 里被 `queue_free()` 的节点在变量里**不等于 `null`**，把它赋给带类型的变量才报错，
  所以 `at:` 指向的常常是一个自身挑不出毛病的调试函数。给出三条配套解法：
  ① 调试/查询 API 返回**值**（名字/字符串）而不是对象；
  ② 采集物销毁前先断链路（关 monitoring + 隐藏），真正的 `queue_free()` 交给
  `SceneTree` 的计时器，别在自己还处在 `await` 调用栈里时销毁自己；
  ③ 持有方每帧 `is_instance_valid()` 兜底 —— **信号会因为"你自己关了 monitoring"而不来**。
  判据：报错行指向"赋值/取属性"且函数本身没毛病时，往上找"谁把已经死掉的节点交出来的"。

## [0.14.0] - 2026-09-18

同一个 Godot 2D 项目做 galgame 风格对话框（打字机逐字淡入、立绘、独立回顾面板）时，
连踩三个**"代码全对、不报错、东西就是不在"**的坑。

### 新增

- `lessons/09` 新增第 15 节：**Godot 4 UI 的三个静默失效（挂上了 ≠ 在生效）**。
  ① `RichTextEffect` 的虚函数在 Godot 4 是 `_process_custom_fx(char_fx)`，
  按 Godot 3 的 `_process_effect(effect, range, char_fx)` 写就等于写了个没人调用的普通方法
  —— 附"给效果加计数器当心跳"的判据；
  ② 容器内的 `TextureRect` 若同时设 `expand_mode=IGNORE_SIZE` 与 `size_flags=SHRINK_*`，
  会被容器按"最小尺寸 0×0"摆放 → 框在、图不在；
  ③ `CanvasLayer.visible = false` 会连带藏掉同层的兄弟面板（`is_visible_in_tree()` 沿父链查），
  表现是"状态是打开的、屏幕是空的" —— 正解是切一个 `Node2D` 子节点的 visible，
  别拿 CanvasLayer.visible 当对话框开关。
  三条都给"断言它真的在动 + 截图人工核对"的验证方式。

## [0.13.0] - 2026-09-18

同一个 Godot 2D 项目进入"把程序化地图换成真正的图块系统"阶段，
踩到三类**只在做图块自动拼接时才会遇到**的坑。

### 新增

- `lessons/09` 新增第 12 节：**从「swatch 图集」派生可自动拼接的 terrain 图集**。
  免费室内图集常是"整块墙带"（3 图块宽 × 2 图块高、四周一圈描边），
  **没有任何一格是单边描边的边/角块** → 直接配 terrain 会拼出一圈圈重复描边。
  记了派生生成器的做法（滑窗挑无描边材质底、取原图描边色、按四邻 16 种组合合成、
  按"上方有没有墙"调明暗做厚度）。
- `lessons/09` 新增第 13 节：**terrain peering bit 的下标是 `TileSet.CellNeighbor` 枚举**，
  不是直觉顺序；给出"先赋 `terrain_set` 再 `is_valid_terrain_peering_bit()` 枚举"的判据，
  以及 `add_terrain_set()` / `add_terrain()` 返回 void、地板的地形位要留空这两个易错点。
- `lessons/09` 新增第 14 节：**沙箱里"文件操作失败"会伪装成"逻辑 bug"** ——
  存档/设置/截图全红而逻辑正常时，先用独立命令确认进程有没有写 `user://` 的权限。
## [0.12.0] - 2026-09-17

一个真实 Godot 2D 项目的首次落地里，**连续踩到五个"逻辑全对、画面全错"的坑**。
这一版把它们全部蒸馏进 `lessons/09`——因为这类 bug 的共性太强：
**所有断言、日志、退出码都正常，只有看画面才能发现**，而"看画面"这条路本身也有陷阱。

### 新增

- `lessons/09` 新增第 8 节：**`get_image()` 全黑的两个前提**。
  - 前提一：**窗口尺寸必须等于视口尺寸**。写了 `window_width_override` 让窗口小于视口时，
    `get_image()` 恒返回全黑，而**同一时刻录帧画面是正常的** → 极易误判成"项目渲染坏了"。
    更坑的是窗口小于视口时 `canvas_items` 不一定缩放，实测出现过"画面 1:1 画在左上角"。
  - 前提二：**Movie Maker 模式吞掉 `--` 之后的自定义参数**，录出来的帧全是开头几帧，
    自动化流程根本没跑。结论：**把抓图做进被测代码，比依赖外部录帧工具可靠**。
- `lessons/09` 新增第 9 节：四个"状态全对但画面不对"的经典坑。
  1. **启动场景从未被释放**：路由首次切场景时内部引用还是 `null`（启动场景绕过路由），
     按节点名兜底又没对上（启动场景叫 `Boot` 不叫 `CurrentScene`）→ 启动 LOGO 永久盖在后续所有场景上。
     正解：用 `get_tree().current_scene`。
  2. **不能对正在执行的场景 `free()`**：实测进程段错误（signal 11）。正解：`remove_child()` + `queue_free()`。
  3. **`fade=false` 路径没清遮罩** → 永久黑屏而逻辑全对。
  4. **代码创建的 Control 调 `set_anchors_preset()` 不生效**（Node2D 根场景），界面全堆左上角。
     正解：显式按视口摆位 + 监听 `size_changed`。
- `lessons/09` 新增第 10 节：**碰撞层是位掩码不是层号**。交互探测 `mask = 4`（本意"第 4 层"）
  实际只匹配第 3 层 → 只能探到 Area2D 交互物、**探不到第 2 层的 NPC**，按交互键没反应。
  附一条高效判据：**"直接空间查询能查到、`get_overlapping_*()` 查不到" ⇒ 几乎必然是 mask 写错**。
- `lessons/09` 第 11 节速查表补齐 6 行（全黑 / 录帧只开头 / 旧场景残留 / 段错误 / 黑屏 / 左上角堆叠 / 交互没反应）。

### 变更

- `lessons/09` 第 7 节顺延为第 8 节之前的位置保持不变，全篇小节编号重排为 1–11。

## [0.11.0] - 2026-09-17

一批"长连接流式 CLI 反复报 stream disconnected"的排查。价值不在结论（服务端容量），
在**怎么用最小代价把"链路断"和"服务端拒"分开**——因为这两类的修复动作完全不相交。

### 新增

- `lessons/14-upstream-model-capacity-vs-network.md`：错误分三类（`at capacity`/`server_overloaded`
  = 服务端；`os error 10054`/`connection reset` = 链路被掐；`403`+封锁页 = 出口地区不支持），
  并给出各自的修复动作。核心判据三条：
  - **40 次探测基准**：成功率 ≥95% 就排除链路，别再碰代理；
  - **看结构化错误码**：日志里的 `server_overloaded` 一出现即定死服务端；
  - **"缩小输入没让问题消失"是排除上下文超限的唯一硬判据** —— 本次直觉误判（小请求过、
    带工具的大请求挂）看起来像超限，把输入缩到 1KB 后照样失败，才推翻了它。
  另含两个坑：内置 provider 段不可覆盖（必须写顶层键）、重试次数解决抖动量而不解决持续过载。

### 变更

- `lessons/README.md`：症状表新增 5 行（流中断 / capacity / 10054 / provider 保留 ID /
  "以为是上下文太大"），主题表加 [14]。

## [0.10.1] - 2026-09-17

同一天的收尾：脉络视角上线后自己复查界面时，发现**起点那行的相关度显示 0**。

### 修复

- `lessons/12` 第 11 节「两条配套的设计结论」补第 3 条：
  **"没参与打分"和"打了 0 分"必须在界面上长得不一样**。起点是用户指定的锚点，
  程序没给它算相关度，于是它带着默认值 `0` 出现在表格第一行 —— 顶着 `★奠基` 徽章、
  旁边写着"相关度 0"，看上去像程序认为这篇奠基工作毫不相关。改成显示 `—`。
  这和上一版的 `to_dict()` 覆盖是同一类错误的两种表现：**空值伪装成 0**。
  差别只在于一个发生在数据层、一个发生在渲染层，所以只修一处不够。

## [0.10.0] - 2026-09-17

给文献工具加"从领域奠基工作按年份往下"的脉络视角。功能是需求，但**经验在"怎么让模型指认领域源头"**上。

### 新增

- `lessons/12` 第 11 节：**让模型指认领域源头，必须过两道闸门**。
  让 LLM 说"这个方向最早的奠基工作是什么"效果出奇地好（一次就点出了该子方向的经典论文），
  但**它给的东西不能直接用**：
  - 闸门一 **存在性**：实测出现过作者串写成 `R. R. C. B. S. et al.`、标题对不上真实论文的情况 →
    拿学术库做标题相似度校验，宁可少几篇也不能把不存在的论文当成起点；
  - 闸门二 **切题**：只校验存在性还不够 —— 一次提名把 `Dynamics of TCP traffic over ATM networks`
    （TCP/ATM 拥塞控制）当成了共享缓冲管理的奠基工作：**真实存在，但属于别的子方向**。
    解法是要求提名必须命中方向关键词若干次。
  另记两条：① **同类实体的兜底要放在唯一的出口上**（检索与定时任务共用一个候选出口，
  在那里过滤一次就够）；② **手填输入的相似度阈值不能比自动提名松** ——
  实测 0.6 会把另一篇论文匹进来。
- 顺带记一个状态展示的坑：**部分构造的对象在序列化时会覆盖掉已算好的值**。
  不生成卡片的路径只构造 `Recommendation(paper=p)`，而它的 `to_dict()` 把
  `score` 写成自身默认值 0，**覆盖**了论文上已算好的分数 → 界面整列相关度显示 0。
  解法是在序列化时对"自身为空"的字段回落到源对象（一行 if，但没想过就会一直错）。

### 说明

- 这一版再次印证一个模式：**模型擅长"给候选"，不擅长"保证正确"**。
  凡是模型给的**标识性内容**（标题、ID、链接、人名）都必须回到确定性数据源校验；
  模型给的**判断性内容**（为什么算奠基、怎么分类）才可以直接用。

## [0.9.0] - 2026-09-17

用户报了一个**元问题**："你编辑文件时总是出现 `file has not been read`，总结经验以后别再出现"。
这不是待修的 bug，而是**我自己的工作方式缺陷** —— 于是这一版把根因、实测矩阵与纪律写成一篇。

### 新增

- **`lessons/13-edit-preconditions.md`：文件编辑的前置条件（根治 "file has not been read"）**。
  全篇基于源码 + 一次跑完的实测矩阵，四个值得单独记的结论：
  - **`write` 不是后门**：未观察的**已存在**文件，`write` 与 `edit` 一样被拒（`createIfAbsent` 守卫）。
    所以"读一次再改"没有合法替代品，别把全量覆盖当逃生舱。
  - **观察的粒度是"文件版本"，不是"行窗口"**：实测 `read` 只读 2 行，随后照样改成功第 21 行。
    → 大文件不必整读；**但"能改"≠"改得对"**，这条省时技巧与第 6 节的警告必须一起用。
  - **没报错的那一半才危险**：这类工具只保证**并发安全（CAS）**，不保证**语义正确**。
    依赖记忆里的旧内容、或只读一个窗口就改全文，工具**照改不误**。
    这与 [01](lessons/01-verification-discipline.md) 的"假通过"是同一件事在编辑场景的投影。
  - **三种报错别修错地方**：`file has not been read`（前置条件）／`file changed since it was read`
    （版本过期）／`[sandbox: ... denied]`（权限）—— 只有前两种能靠"重读"解决，
    对权限报错反复重读是白烧重试预算。
  另附：观察按**会话**隔离（跨会话/跨工作区编辑必踩）、`grep`/`glob`/shell 读**不算读**、
  以及一段可直接抄进系统提示词的写法。

### 说明

- 触发场景被明确归为三类，覆盖了绝大多数复发：**上个会话碰过的文件**、**只用 grep/shell 看过**、
  **外部工具刚改过**。这三类都不是"工具坏了"，而是"我少调了一次 read"。
- 索引同步：`lessons/README.md`（症状表置顶两行 + 主题表加 13）、`lessons/07` 速查表加一行。

## [0.8.0] - 2026-09-17

用户报了两个 bug，都是"用久了才会撞上"的类型。

### 新增

- `lessons/12` 第 10 节：**推荐系统必须把用户已经知道的东西排除掉**。
  用户给的种子论文只被用来推导检索方向，却**从来没被用来过滤结果** ——
  而种子恰恰是检索式最容易命中的（它们就在这个方向正中央），
  于是系统把用户自己给过的论文当成新结果推回去、甚至发进每日邮件。
  给出解法（多种标识归一化 + 在唯一候选出口过滤）与三个细节：
  标题必须归一化（`{}`/`:` 都会被压成空格）、
  **正则别把 DOI 里的数字读成 ID**（`10.1145/3689031.3717495` 能匹配出 `9031.37174`，
  要靠"年月必须合法 + 边界"排除）、顺带排除"用户已在别处收藏的"。
  点出普遍性：**任何"用户给几个样例 → 找相似的"功能都有同一个洞**。
  另记一个连带损失：种子挤占每日 top_n 名额，用户因此少收到一篇真正的新论文。

- `lessons/12` 第 9 节新增一行：**服务端 404 但文件明明在磁盘上** ——
  文件名含中文/空格，浏览器发的是百分号编码路径，服务端没解码就去拼磁盘路径。
  这个 bug 在代码里躺了好几天，只是之前没人点过带中文名的报告链接。

## [0.7.0] - 2026-09-17

一次"用户又没收到定时邮件"的排查。故障本身在用户的代理软件上，
但**为了修它，在自己代码里挖出三个真 bug** —— 这一版记的是后者。

### 新增

- `lessons/01` 事故 8：**`UPDATE ... WHERE` 匹配 0 行是静默成功**。
  我拿一个"库里的旧指纹"去更新状态，代码无异常、日志说"已处理 2 篇"，
  而数据一点没变（同一篇论文这轮多了 DOI，指纹从 `title:` 变成 `doi:`，
  "把新指纹对齐到旧记录"这一步发生在这段代码之后）。给出三层解法：
  **写操作必须返回受影响行数**、调用方必须对 0 行做出反应、
  并提供一个不依赖易变标识的定位方式。
- `lessons/01` 事故 9：**候选"落选后不留痕"会让队列永不收敛**。
  候选 = "没有任何记录的实体"，落选的不写记录 → 下一轮仍算新候选 →
  每天重复抓取 + 评分，永远没有新内容，而单看任何一天都"工作正常"。
  给出状态机设计（`pending` / `sent` / `shown` / `dropped`）与**唯一有效的验证方式**：
  **连跑两次，断言第二次候选数为 0 且没有付费调用**。
  附一个连带坑：把"发信失败"退回成"用户看过"那个状态是错的 ——
  **"可重试"必须是一个独立状态**，否则既不会重发也不会被清理。
- `lessons/11` 第 10 节：**代理软件的 TUN 模式会让非 HTTP 协议莫名失败**。
  症状是 HTTPS 全正常，但 SMTP / IMAP 等端口 TLS 超时（换服务商、换端口、
  连真实 IP 都失败）。根因是 fake-IP 的**第二层**：域名被解析成 `198.18.x.x`，
  代理靠内部映射还原域名来匹配规则；映射缺失时（代理刚重启、而系统 DNS 缓存里
  还留着上一轮的假 IP）域名规则全部失效 → 掉进兜底规则 → 节点封 SMTP 端口。
  关键鉴别点：**`DOMAIN-SUFFIX,<域名>,DIRECT` 明明写着却依然走代理 ——
  规则没错，错的是"域名还原"这一步，别去改规则顺序**。
  附一条反直觉提醒：故障会自己变化（同一段代码几分钟后结论相反），
  所以要以"修好之后能否持续成功"为准。

### 说明

- 这三条都来自"修别处的问题时，顺手把自己的代码查了一遍"。
  用户的故障在代理软件里，但排查过程暴露的三个缺陷**是我们自己的状态机问题**，
  其中两个会**持续产生成本**（每天重复的付费调用）或**永久丢数据**（论文再也不会重发）。
  记录的价值高于修复本身。

## [0.6.0] - 2026-09-16

给一个文献聚合系统加"记住高分论文"，结果把这个项目里**潜伏最久的两个数据缺陷**挖了出来。
两条都是"单次运行测不出来、随时间浮现"的类型，值得单独成节。

### 新增

- `lessons/12` 第 7.1 节：**批次内去重 ≠ 库级去重 —— 跨运行身份会重新裂开**。
  指纹做主键的聚合系统，第 1 天由无 DOI 的数据源、第 2 天由带 DOI 的数据源命中同一实体时，
  会落成两行。给出解法（库级归一化标题索引 + 模型支持**显式覆盖主键** `canonical_key`）、
  修历史数据的一次性作业写法，以及搬迁子表时**单列主键 vs 复合主键**的处理差异
  （前者 `INSERT OR IGNORE + DELETE`，后者必须 `INSERT OR IGNORE ... SELECT`）。
  附三条验证要求，其中一条是"子表零孤儿 + 作业幂等"。
- 同节补充"**合并字段要按质量取优，不能先到先得**"：真实案例是合并后留下了预印本
  venue，导致「发表在哪里」长期显示 arXiv 占位名而不是正式会议名；
  并强调 upsert 时也要"只升不降"，否则后续某次只有预印本源命中时会再次降级。
- `lessons/12` 第 7.2 节：**身份保证要放在存储层，不要只放在某一个调用方里**。
  只在"检索一次"这个编排函数里对齐 key，等于把不变量押在"所有调用方都记得先对齐"上；
  写入路径越多（UI / CLI / 回填 / 定时任务），漏掉一条只是时间问题。
  给出把兜底放进 `upsert()` 的写法，以及一条容易踩的副作用：
  **兜底加上后"造重复行"本身变难了**，原来测修复作业的用例会因为造不出脏数据而失败 ——
  那不是测试坏了，而是修复生效的证据；正确做法是改用原始 SQL 伪造历史脏数据继续覆盖。
  同节补"只升不降"的两个同类字段（出版社 DOI > 仓储 DOI、正式会议名 > 预印本占位名），
  并强调**合并与落库两处都要做**。
- `lessons/12` 第 9 节新增两行：**"只读"采集函数 + 另一个调用方** 会导致写子表却不写主表
  （留下孤儿行，界面用 INNER JOIN 时还看不见）；以及**阈值类配置要可调且"调高不删数据"**
  （删除必须是显式动作）。

### 说明

- 这两个缺陷都不是靠"再读一遍代码"发现的，而是**因为新功能把它们照出来了**：
  新做的"高分记忆"清单把同一篇论文的两行并排列出，重复一眼可见。
  经验：**当一个新功能会把隐藏数据的聚合视图暴露给用户时，它同时是一次免费的数据审计。**
  遇到"新功能一上线就看到脏数据"，先别急着在新功能里加过滤 —— 那通常是在掩盖根因。

## [0.5.0] - 2026-09-16

一次真实的"用户没收到定时邮件"故障排查，产出两条新经验。

### 新增

- `lessons/02` 第 10 节：**`schtasks /Create` 的默认设置会让笔记本静默跳过定时任务**。
  三个默认值（`StartWhenAvailable=false`、`DisallowStartIfOnBatteries=true`、
  `StopIfGoingOnBatteries=true`）对笔记本全是错的，而 `schtasks.exe` **没有任何开关能改它们** ——
  所以"用 schtasks 注册"这条路本身就不够。给出 `Register-ScheduledTask` 的正确写法、
  注册后必做的回读、以及一条三步排障顺序（查任务信息 → 查日志 mtime → 查业务产物）。
  重点强调：**这个失败不产生任何报错**，任务只是"没发生"，排查者最容易往脚本/网络/凭据上找。
- `lessons/01` 事故 7：**空结果和探测失败长得一模一样**。
  我用 `Get-ScheduledTask -ErrorAction SilentlyContinue` 得到空列表，差点下结论"任务不存在了"；
  实际是当时沙箱限制了任务计划程序接口，错误被 `-ErrorAction` 吞掉了。
  救回来的是**对照实验**：对一个已知存在的样本和一个已知不存在的样本各跑同一次探测，
  三者表现一致就说明是工具坏了。附两条通用做法：排查阶段别用 `SilentlyContinue`；
  多准备一条独立取证路径（这里用磁盘上的任务定义 XML 绕过接口）。

### 说明

- 这两条合起来是一个完整闭环：**故障本身**（第 10 节，产品默认值问题）
  与**排查故障时的方法**（事故 7，观测工具也会骗你）。
  真实故障的排查过程比单纯实现功能更能产出可复用结论。

## [0.4.0] - 2026-09-15

这一版全部来自同一次实现：给一个本地 Web 服务做"桌面一键打开"。新增的都是**验证方法**层面的经验。

### 新增

- `lessons/01` 事故 6：**验证"打开了网页/窗口"时的四种假证据**。
  一张表列出我实际拿到的四个"看起来通过了"的观测，其中三个是假的
  （`cmd /c start` 对 `.lnk` 经常静默不启动；已有浏览器实例开新标签不新建进程也不改命令行；
  "已打开"的日志是过程证据）。给出可复用的解法：**把观测点放在因果链末端** ——
  给被测服务加访问日志，清空 → 触发 → 看请求是否真的到达。
  附结论：模拟双击要用 ShellExecute（`Start-Process <.lnk>`）而不是 `cmd start`；
  以及"先怀疑测试手法，再怀疑被测对象"。
- `lessons/02` 第 2 节新增**反向坑**：不只"别写出 BOM"，**读配置时也必须容忍 BOM**
  （`encoding="utf-8-sig"` + 把 JSON 语法错误转成人话）。
  这个坑的隐蔽性在于报错指向"第 1 行第 1 列"，看起来像 JSON 写错了，很难联想到 BOM。
  附证伪式验证写法：**故意造一个带 BOM 的副本**再读，而不是只读自己的文件。
- `lessons/02` 新增 9.1 / 9.2：把"纯 ASCII"从**记忆**变成**机械校验**。
  含可直接复制的字节扫描脚本，以及一条真实复发记录 ——
  这条规则早就写在库里，我依然又犯了两次（中文出现在用法示例里；
  `.bat` 里把中文 `echo` 放在 `chcp 65001` 之前）。
  另注明 `ParseFile` 报 OK **不代表**在目标 shell 能跑，两项检查必须都做。

### 说明

- 0.1 → 0.3 的经验都来自"把事情做成"；这一版来自"**验证自己有没有做成**"。
  两者比重不同：后者更省时间，因为它阻止你花两小时去修一个本来没坏的东西。

## [0.3.0] - 2026-09-15

新增一篇主题经验（学术数据源），并给 MCP 桥接篇补了一条"改了不生效"的排查法。

### 新增

- `lessons/12-literature-source-apis.md`：Google Scholar / arXiv / dblp / OpenAlex /
  Crossref 五个源的真实可用性与各自的对策，外加跨源去重、Zotero 写入幂等两节。
  核心结论用的是**可用性差异表 + "让每个源独立失败"的架构推论**，而不是罗列 API 参数。
  含一条可复用的纯函数（标题相似度闸门）与它的阈值选择依据。
- `lessons/04` 第 8 节：**桥接源码改对了但工具仍报旧错误**。
  给出 30 秒判据（对比文件 mtime 与宿主进程 StartTime、全盘找副本、临时 stderr 探针），
  以及"修完立刻验证错误是否变化"的收尾规范。这是第 6 节"注册只在启动时组合一次"的
  具体化 —— 之前只说了"注册表"，没说"**已加载的模块**同样不会热更新"。

### 索引

- `lessons/README.md`：11 → 12 篇；「按症状查」新增 4 行（桥接旧代码、学术 API 429/反爬、
  多源重复、按标题匹配错文章）。

## [0.2.0] - 2026-09-15

发布前做了一次**对抗式独立审查**（由一个不共享上下文的审查者通读全部 44 个文件），
按结果修掉了一批"文档与代码不一致"和两处环境指纹。**这一版才是真正可复用的一版。**

### 修复（会让复用者第一次接触就失败的问题）

- `recipes/README.md` 里 `zotero-mcp` 的依赖写成 Node 18+，实际应为 **Node 22+**
  （代码用了内置 `node:sqlite`）。同一声明在两个文件里互相矛盾。
- `mcp-bridge-minimal/README.md` 把**源码常量名**（`SERVER_NAME`/`TOKEN_FILE`/`TRACE_FILE`）
  当成**环境变量名**列出，而代码读的是 `BRIDGE_*`。现已拆成"源码常量 / 环境变量"两张表，并显式提示两者不是同一字符串。
- `godot-mcp/README.md`、`zotero-mcp/README.md` 同类问题（`BRIDGE_PORT`、`LIBRARY_SCOPE`），
  同样拆表并补上此前**完全没提到**的 `GODOT_STEAM_ROOTS` / `GODOT_SCAN_ROOTS`。
- `godot-mcp/README.md` 声称 20 个工具、代码只注册 19 个：`godot_editor_reload` **实现了却漏在
  `SPECS` 之外**，调用方永远够不到。已补注册（现为真实的 20 个），并在 README 写明
  "只写实现不注册等于没做"这个坑。

### 变更（安全）

- `start-phone-access.ps1` 原来**按进程名扫杀所有 `node.exe`** 来清理孤儿 MCP 子进程 ——
  这正好违反了本库自己写在 `AGENTS.md` 里的硬规则 #1，而且会毁掉宿主自己的后台任务运行器。
  改为**只清 `ParentProcessId` 等于"刚被停掉的宿主 PID"的进程**，
  并叠加命令行关键字二次收窄。README 新增第 3 节说明这个约束与判据。
- 同一脚本的"端口在监听 → 认为网关已在运行"改为**解析占用端口的 PID 并核对命令行**。
  端口通只能证明"有东西在监听"，不能证明那是你的服务（这正是 `lessons/09` 第 3 节的坑）。
  冲突时**明确报错退出**，不再假装成功。

### 变更（可移植性）

- `godot-mcp.mjs`：Steam 库根目录与默认扫描根改为环境变量可覆盖；
  `godot_install_addon` 里一处**路径字符串拼接错误**（本该是 `join()`）已修。
- `dsh_bridge.gd`：端口范围改为读 `DSH_GODOT_BRIDGE_PORT` / `_END`，
  且端口全占用时报错文案明确给出解决办法（原先只有一行 stderr 就静默不启动）。

### 变更（脱敏）

- 移除三处**环境指纹**：具体 GPU 型号与渲染分辨率、具体浏览器版本号、第三方安全软件品牌名。
  经验结论不变，但不再指向某一台可识别的机器。
- `README.md` 的隐私声明改为**准确表述**：不含**身份信息**，但会保留**技术层面的环境描述**
  （保留是因为它们是经验有效的前提），并说明泛化 / 标注 / 可覆盖三条处理原则。
  同时明确"发现硬件指纹属发布事故，请开 issue"。

### 新增（经验条目）

- `lessons/11` 新增第 8 节「**认证类 403 的分层诊断**」：以 `git push` 被拒为例，
  给出"读通过 + 写 403 ⇒ 凭据权限问题"这条判据、用 `--dry-run` 与
  `-c credential.helper=` 逐层排除干扰的步骤，以及一个真实踩到的陷阱 ——
  **GitHub API 返回的 `permissions.push` 描述的是"用户在仓库里的角色"，不是"token 的授权范围"**，
  拿它判断 token 能否推送会得出完全错误的结论。

### 工程

- `publish.ps1` 修掉一个真实缺陷：**工作树干净时它不会推送已有提交**。
  这会让定时任务永远推不动手工提交。现在会检查 `@{u}..HEAD` 并推送未上行的提交。

## [0.1.0] - 2026-09-15

首个版本。建立仓库结构，并把此前若干天里验证过的结论沉淀成体系。

### 新增

**经验条目（`lessons/`）**

- `01-verification-discipline.md` —— 验证纪律：怎么避免"假通过"，怎么构造能否证伪的断言。
  含真实事故两个（`delete eval(...)` 无效操作、把 `[本机]` 当 `[通病]` 照抄）。
- `02-windows-powershell51.md` —— PowerShell 5.1 的四个坑：无 BOM 脚本被按 GBK 解码、
  `Set-Content -Encoding utf8` 写 BOM 毒害下游 JSON、`Add-Type` 与解析期类型字面量、
  `Invoke-WebRequest -MaximumRedirection 0` 的异常语义。另含 WMI 脱离进程树启动的实测矩阵。
- `03-sandbox-stdio-limits.md` —— 沙箱禁止管道 stdio 的连锁后果（`spawn` EPERM、`git push` 必失败、
  `cmd | Select-Object` 失败），为什么"宿主侧跑一个 stdio 服务"是正确的破局点，以及
  "绝不批量 kill 宿主进程"的事故复盘。
- `04-mcp-stdio-bridge-authoring.md` —— 从零写 MCP stdio 服务的范式：握手、`tools/list`、
  `tools/call`、错误该返回还是该抛、日志为什么必须走 stderr、工具命名的宿主约定，
  以及用 JSONL 落盘做可复现调试的方法。
- `05-browser-automation-playwright.md` —— 用 Playwright 驱动**系统已装浏览器**（不下载 Chromium）、
  持久 profile 保存登录态、快照优先于截图、`addInitScript` 模拟旧内核的验证技巧。
- `06-legacy-engine-compat-shim.md` —— 依赖库探测缺失 API 的方式有缺陷时如何整包炸掉
  （`Iterator.prototype` 在判断条件里被访问却没有存在性检查），以及网关层注入兼容垫片的
  正确形态：逐项存在性判断、自我报告补了哪些、对现代引擎零副作用。
- `07-dsh-harness-internals.md` —— Cordis patch 的语义边界（`name` 是断言不能换包、
  `overrides` 会直接赋值所以 `disabled` 可用）、"只在启动时组合"的体系有哪些、
  目录选择器 native/browse 的互斥设计。
- `08-lan-reverse-proxy-remote-gui.md` —— 把只监听回环的 GUI 暴露到局域网的完整方案：
  为何不能直接改 bind 地址、反代必须重写哪些头才能过信任围栏、SSE/WebSocket 转发、
  PWA manifest 不带 cookie 导致的口令门漏网、压缩与 HTML 改写冲突。
- `09-godot-automation.md` —— 无头模式没有渲染（要画面必须 Movie Maker）、
  端口被无关系统服务占用的探测陷阱与自动挑端口方案、临时目录用点号前缀避免被资源扫描。
- `10-zotero-automation.md` —— 本地 API 默认关闭且只读、运行中 SQLite 被独占锁的应对
  （复制快照读）、写操作走官方 Web API、连接器接口只能新增不能改。
- `11-http-network-troubleshooting.md` —— 同一台机器上不同 HTTP 客户端的连通性可以不一致，
  排查时先换客户端再怀疑网络；以及 TLS/代理排查的正确顺序。

**配方（`recipes/`）**

- `mcp-bridge-minimal/` —— 最小可跑的双向 stdio MCP 服务模板（零依赖，单文件）。
- `compat-shim/` —— 可独立使用的旧内核兼容垫片。
- `git-mcp/` —— Git / GitHub MCP 服务：认证、仓库创建、提交、推送（凭据不进 argv、不进仓库配置）。
- `zotero-mcp/` —— Zotero 读写 MCP 服务（SQLite 快照读 + 官方 Web API 写）。
- `godot-mcp/` —— Godot MCP 服务（CLI 桥 + 编辑器插件桥，含 GDScript 插件）。
- `phone-gateway/` —— 局域网反向代理网关（HTTP/SSE/WebSocket + 口令门 + 兼容垫片注入）。
- `windows-launcher/` —— Windows 启动器与二维码生成脚本。

**工具（`tools/`）**

- `publish.ps1` —— 发布流水线：同步配方源码 → 脱敏扫描（硬门禁）→ 提交 → 推送。
- `install-scheduled-task.ps1` —— 安装/卸载定期更新的计划任务。
- `redact-rules.json` —— 脱敏规则表（占位符映射 + 禁止模式黑名单）。
